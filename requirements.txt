# Python dependencies for Bodet Scorepad Capture
# Python 3.6+ required

# No external dependencies required for basic functionality
# Standard library only: socket, threading, queue, configparser, json, logging
